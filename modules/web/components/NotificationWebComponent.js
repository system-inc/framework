NotificationWebComponent = WebComponent.extend({

	attributes: {
		class: 'notification',
	},

	show: function(text) {
		//this.setContent(text);
		this.show();
	},

	hide: function() {
		this.hide();
	},

});